// import { seedTerorEvents } from "../utils/enterSid";

export const seed = async () => {
    try {
        // await seedTerorEvents()
        console.log("finish to enter the seed")
        return 
    } catch (error) {
      console.error("Error in seed:", error);
      throw error 
    }
  };