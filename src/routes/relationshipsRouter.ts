import express from "express";

const router = express.Router();

router.get("/top-groups/:aria", ()=>{});
router.get("/groups-by-year/:yyyy" , ()=>{});
router.get("/deadliest-regions/:name_group",  ()=>{});

export default router;
